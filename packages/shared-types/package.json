{
  "name": "@auth-sandbox-2/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --eval \"console.log('shared-types: no tests')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format:check": "node --eval \"console.log('shared-types: format check skipped')\""
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
